@model IEnumerable<BookStore_ph1.Models.BookGenre>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Book)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Book.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre.GenreName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BookGenreId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BookGenreId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BookGenreId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
